  #!/bin/bash
  wget http://127.0.0.1:15672/cli/rabbitmqadmin;sudo rabbitmqctl set_permissions -p / test ".*" ".*" ".*";python rabbitmqadmin declare queue name=tlantic_area_queue durable=true;python rabbitmqadmin declare queue name=tlantic_poi_queue durable=true;python rabbitmqadmin declare queue name=tlantic_pushnotification_queue durable=true;python rabbitmqadmin declare queue name=tlantic_email_queue durable=true;python rabbitmqadmin declare queue name=tlantic_retail_queue durable=true;python rabbitmqadmin declare exchange name=tlantic_ex_feed type=topic;python rabbitmqadmin declare exchange name=tlantic_ex_notification type=topic;python rabbitmqadmin declare exchange name=tlantic_ex_retail_queue type=topic;python rabbitmqadmin declare binding source=tlantic_ex_feed destination_type=queue destination=tlantic_area_queue routing_key=area;python rabbitmqadmin declare binding source=tlantic_ex_feed destination_type=queue destination=tlantic_poi_queue routing_key=poi;python rabbitmqadmin declare binding source=tlantic_ex_notification destination_type=queue destination=tlantic_pushnotification_queue routing_key=email;python rabbitmqadmin declare binding source=tlantic_ex_notification destination_type=queue destination=tlantic_poi_queue routing_key=push;python rabbitmqadmin declare binding source=tlantic_ex_retail_queue destination_type=queue destination=tlantic_retail_queue routing_key=detection; mkdir -p work/files/error; mkdir -p work/files/in; mkdir -p work/files/out